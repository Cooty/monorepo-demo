{
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "test": "lerna run test",
        "dev": "lerna run dev --scope=@monorepo-demo/app",
        "new-version": "lerna version",
        "diff": "lerna diff"
    },
    "devDependencies": {
        "lerna": "^4.0.0"
    },
    "name": "monorepo-demo"
}
